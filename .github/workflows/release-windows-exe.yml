name: Release Windows EXE

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build-windows-exe:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Installa build deps
        run: |
          python -m pip install --upgrade pip
          pip install -r python-app/requirements-build.txt

      - name: Build EXE (PyInstaller)
        run: |
          cd python-app
          pyinstaller --noconfirm --clean --onefile --name "AccessibileDaZero" accessibile_da_zero/cli.py
          pyinstaller --noconfirm --clean --onefile --windowed --name "AccessibileDaZeroGUI" accessibile_da_zero/gui.py

      - name: Prepara ZIP
        run: |
          cd python-app/dist
          powershell -NoProfile -Command "Compress-Archive -Path .\\AccessibileDaZero.exe,.\\AccessibileDaZeroGUI.exe -DestinationPath .\\AccessibileDaZero-windows.zip -Force"

      - name: Upload artifact (fallback)
        uses: actions/upload-artifact@v4
        with:
          name: AccessibileDaZero-windows
          path: |
            python-app/dist/AccessibileDaZero.exe
            python-app/dist/AccessibileDaZeroGUI.exe
            python-app/dist/AccessibileDaZero-windows.zip

      - name: Crea Release e carica file
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            python-app/dist/AccessibileDaZero.exe
            python-app/dist/AccessibileDaZeroGUI.exe
            python-app/dist/AccessibileDaZero-windows.zip
          generate_release_notes: true
